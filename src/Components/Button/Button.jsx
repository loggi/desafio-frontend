// import { StyledButton } from "./Styled";

// const Button = ({bgColor, textColor, children}) => {
//     return <StyledButton bgColor={bgColor} textColor={textColor}>{children}</StyledButton>;
//   };
  
//   export default Button;